# Supabase Backup Configuration

# Project directory (absolute path to your Supabase project)
# Leave empty to use current directory
PROJECT_DIR=""

# Backup directory (relative to PROJECT_DIR or absolute path)
BACKUP_DIR="./backups"

# Number of days to keep backups (older backups will be deleted)
KEEP_DAYS=7

# Database container name
DB_CONTAINER="supabase-db"

# Database user
DB_USER="postgres"

# Log file location (relative to PROJECT_DIR or absolute path)
LOG_FILE="./backups/backup.log"
